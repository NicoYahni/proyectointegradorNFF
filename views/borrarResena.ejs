<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class= "Delete">
        <% if (usuarioLogeado! = null){ %>
        <h1> Are you sure you want to delete this review? </h1>
        <p><%= resultado.resena%></p>
        <form name= "resenas" methos= "POST" action="/playit/delete/<%=resultado.id%>" >
        
        <p><% resultado.resena%></p>

        <button class="uk-botton enviar"> DELETE </button>
        </form>



    </div>
</body>
</html>

router.get ('/reviews/delete/:id', controller.borrarResena);
router.post ('/reviews/delete/:id', controller.confirmDelete);