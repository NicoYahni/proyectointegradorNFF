<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Usuario Buscado</title>
</head>
<body>
    <h1>Usuarios buscados:</h1> 
    <% if (users.length > 0) { %>
    <% for(let i=0; i < users.length; i++) { %>
    <h3><a href="/userDetail/<%= users[i].id %>"><%= users[i].nombreCompleto %></a></h3>
    <h4>Email: <%= users[i].email %></h4>
    <hr>
    <% } %>
    <% } else { %>
        <p>No hemos encontrado usuarios con esa busqueda</p>
        <% } %>
    

</body>
</html>